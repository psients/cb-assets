# cb-assets

Image host for the CB Marketing publishing pipeline.

Instagram's API cannot accept file uploads — it only fetches images from a public
URL. This repo is that URL. `cb-publish` writes rendered post images here, pushes,
and hands Meta the raw.githubusercontent.com link.

Not a website. Nothing here is meant to be browsed.
